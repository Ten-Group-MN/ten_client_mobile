import 'package:flutter/material.dart';

class StartsScreen extends StatelessWidget {
  const StartsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
